let userInput = prompt("이름을 입력하세요.");
// conditional-1의 조건문에서 null값만 제외한다는 조건으로만 구성하여 짧게 쓰기
if (userInput !== null) {
  alert(userInput);
}

// 사용자의 다른 패턴에 대한 피드백이 필요하다면 else문을 쓸 필요가 있다

// 사용자가 늙어.. 젊은층 UX만 반영하는 식으로 생각하기에는 우리회사에 수익창출을 해주는 사용자 계층은 디지털 취약계층일 수 있다
// UI개선이라는 것을 할 때, 연장자 층도 고려해야한다.
